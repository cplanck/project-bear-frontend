import ReactCropImage from './AvatarImageSelector'
import styles from '../../components/instrument/Instrument.module.css'

export default function Image(){
    return(
            <ReactCropImage/>
    )
}